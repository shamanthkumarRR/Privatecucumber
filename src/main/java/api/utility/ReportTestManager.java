package api.utility;

//import com.aventstack.extentreports.ExtentReports;
//import com.aventstack.extentreports.ExtentTest;


public class ReportTestManager {
	/**
	//static Map<Integer, ExtentTest> extentTestMap = new HashMap<Integer, ExtentTest>();
	//static ExtentReports extent = WebBaseClass.getInstance();
	
	/**
	 * It will return test name
	 * 
	 * @return
	 */
	/**
	//public static ExtentTest getTest() {
	return extentTestMap.get((int) Thread.currentThread().getId());
	}
	
	/**
	 * Add test in extent report listener
	 * 
	 * @return
	 */

	/**
	public static ExtentTest startTest(String testName, String className, String description) {
		ExtentTest test = extent.createTest(testName, description);
		extentTestMap.put((int) Thread.currentThread().getId(), test);
		test.assignCategory(className);
		return test;
	}
	
	public static void addPriority(int priority) {
		getTest().assignDevice("Priority&nbsp;:&nbsp;" + priority);
	}
	 **/
	
}
